"use client";

import { PlanningStub } from "@/components/planning-stub";

export default function WebShellPage() {
  return <PlanningStub title="WebShell" description="攻陷主机终端（WebSocket + PTY，HITL 门控），规划中。" />;
}
